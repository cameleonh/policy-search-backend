[project]
name = "packages-contracts-py"
version = "0.0.0"
description = "Shared Python contracts (Pydantic models) for cross-service data exchange"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9"]
