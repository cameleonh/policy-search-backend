FROM python:3.12-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml uv.lock alembic.ini ./
COPY apps/api/ apps/api/
COPY workers/ workers/
COPY packages/ packages/
COPY db/ db/
RUN uv sync --no-dev

CMD ["echo", "Ingest worker ready. Run manually: uv run python -m workers.ingest.main"]
